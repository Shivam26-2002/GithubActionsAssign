name: condition
on:  workflow_dispatch
jobs:
  first:
    runs-on: ubuntu-latest
    steps:
      - name: Trial step
        id: trial
        run: |
          exit 1
      - name: Imp step
        if: steps.trial.outcome == 'success'
        run: echo "Trial step succeded"
  
