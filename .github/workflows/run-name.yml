name: Endeca Deploy ETL
on: 
  workflow_dispatch:
    inputs:
      environment:
        required: true
        type: choice
        description: Select the environment
        options:
          - 'dev'
          - 'test'
          - 'prod'
          - 'ubuntu-latest'
        default : 'dev'

run-name: Endeca Deploy ETL - ${{ inputs.environment | upper() }}

jobs:
  build-and-deploy:
    # runs-on: [self-hosted, X64, Linux, endeca, rhel8, "${{ inputs.environment }}" ]
    runs-on: [ "${{ inputs.environment }}"]
    steps:
      - name: Dummy workflow
        run: echo "Dummy workflow"
