name: Actor Schedule trigger
run-name: Schedule Actor
on: 
  schedule:
    - cron: '*/10 * * * *'

jobs:
  dev_job:
    name: Actor name
    runs-on: ubuntu-latest
    steps:
      - name: First Step for DEV
        run: echo "This is the test first step for DEV"
        
  dev_job2:
    runs-on: ubuntu-latest
    steps:
      - name: Second Step for DEV
        run: echo "This is the test first step for DEV"
