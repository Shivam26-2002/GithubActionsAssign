name: Workflow run 1
on:
  workflow_dispatch:
jobs:
  start1:
    runs-on: ubuntu-latest
    steps:
    - name: Write
      run: echo "Hello Workflow 1"
    - name: Write this in 
      run: echo "*By:* ${{ github.actor }} *Branch:* ${{ github.event.ref }} *Short SHA:* ${{ github.sha | slice(0, 7) }} *Date:* $(date -d "${{ github.event.head_commit.timestamp }}" '+%Y-%m-%d') *Time:* $(date -d "${{ github.event.head_commit.timestamp }}" '+%H:%M')"

