name: A
on: 
  workflow_dispatch:
    inputs:
      environment:
        required: true
        type: choice
        description: Select the environment
        options:
          - 'Testing'
          - 'ole'
        default : 'DEV'
  push:
jobs:
  D:
    runs-on: ${{ ( github.event_name == 'workflow_dispatch' && 'ubuntu-latest' || 'windows-latest' ) }}
    # environment:  ${{ ((inputs.environment == 'Testing' || inputs.environment == 'ole') && 'Testing') || 'jlg' }}
    steps:
      - name: Extract Test Results
        id: extract-report
        run: |
          if [[ ${{github.event_name}} == 'workflow_dispatch' ]]; then
            echo "Worked"
          else
            echo "PUSH"
          fi 
          
           
