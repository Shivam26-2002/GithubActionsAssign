name: Schedule trigger
on:
  schedule:
    - cron: '*/5 * * * *'
    # - cron: '53 11 * * *' # 5:30 PM every day
jobs:
  schedule:
    runs-on: ubuntu-latest
    steps:
      - name: Dummy job
        run: echo "Dummy step"
