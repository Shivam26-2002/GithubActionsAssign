name: Strategy Matrix
run-name: Strategy matrix - ${{ matrix.os }}
on: workflow_dispatch
jobs:
  Test-Strategy:
    strategy:
      matrix:
        os: [1, 2, 3]
    runs-on: ubuntu-latest
    steps:
      - name: Print the OS
        run: |
          echo "Matrix is : ${{ matrix.os }}"
